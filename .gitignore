# Root compose configuration is per machine; commit .env.example instead.
/.env

# PHPUnit writes these on every run; they are per-machine state, not source.
/app/.phpunit.result.cache
/app/.phpunit.cache/

# Local SQLite databases (the test profile writes app/var/test.db).
*.db
